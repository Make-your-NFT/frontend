import styles from "components/main/intro.module.css";

const Intro = () => {
  return (
    <div className={styles.layout}>
      <h1>당신만의 NFT로 옷을 만들어보세요</h1>
    </div>
  );
};

export default Intro;
